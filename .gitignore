# Python
__pycache__/
*.py[cod]
.venv/
.pytest_cache/
.ruff_cache/
*.egg-info/
dist/
build/

# Node (an admin/frontend UI, if one is added later)
node_modules/
.next/
out/

# Terraform
.terraform
.terraform.lock.hcl
.terraformrc
terraform.rc

# Env / local
.env
.env.*
!.env.example
*.tsbuildinfo

# Per-unit-of-work worktrees (AGENTS.md §1). Git-ignored so they are never
# committed and never double-scanned by a linter or type-checker.
.worktrees/
